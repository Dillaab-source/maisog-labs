{
  "contract_schema_version": "1.0.0",
  "task_id": "TASK-EXAMPLE-INVALID-DEPLOYED-RUNTIME-ALONE",
  "title": "Invalid: a DEPLOYED claim requires only RUNTIME_OBSERVED, which does not guarantee ACTOR_REPORTED or CI_ATTESTED",
  "project": "Dillaab-source/maisog-labs",
  "change_class": "ARCHITECTURE",
  "authorization_references": ["ML-DEVOS-RFC-013"],
  "scope": {
    "allowed_paths": ["worker/"],
    "remote_resources_involved": true,
    "protected_main_or_deploy_in_scope": true,
    "production_write_in_scope": true,
    "credential_or_security_in_scope": false,
    "destructive_actions_in_scope": false
  },
  "acceptance_criteria": [
    { "criterion_id": "AC-1", "statement": "N/A -- this fixture exists only to prove CORE-017 fails closed when a DEPLOYED claim's ONLY required evidence class is RUNTIME_OBSERVED, which never guarantees ACTOR_REPORTED or CI_ATTESTED (AS54-F003/F005: the failure reason is the missing guaranteed class, never a ban on RUNTIME_OBSERVED itself -- see the valid deployed-claim-stronger-with-runtime-observed.contract.json fixture, where the SAME class is present alongside a guaranteed one and is accepted)." }
  ],
  "claims": [
    {
      "claim_id": "CLAIM-1",
      "claim_kind": "DEPLOYED",
      "statement": "This change was deployed, evidenced only by RUNTIME_OBSERVED with no guaranteed ACTOR_REPORTED or CI_ATTESTED path.",
      "evidence": { "all_of": ["RUNTIME_OBSERVED"], "any_of": [] }
    }
  ],
  "authority_disclaimer": "This Task Contract describes already-authorized scope. It does not itself grant authority, tool access, credentials, remote-resource access, merge approval, deployment approval, or risk acceptance (CORE-001, CORE-002). Satisfying this contract's acceptance criteria and evidence requirements does not certify task success or accept the task; that judgment belongs to the Architect/Paulo review that consumes this contract's evidence, not to the contract or its validator."
}
