# Waivers

Active and historical waivers live here as `ML-DEVOS-WAIVER-<NNN>.md`, using `../../templates/WAIVER_TEMPLATE.md`. Numbering is sequential starting at `001`, never reused. Every waiver **must** carry a mandatory `expires_at` — there are no permanent silent exceptions (`../../governance/change-policy/CHANGE_GOVERNANCE_POLICY.md` §1, `WAIVER` row).

No waiver has been filed as of S1.
