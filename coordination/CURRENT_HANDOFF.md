# Current Handoff — RFC-020 Stage B Atomic Protocol V2 Activation (D-080)

```yaml
schema_version: 1
handoff_id: H-RFC020-STAGE-B-ACTIVATION-0001
cycle_id: MAISOGLABS_CANONICAL_DIRECTIVE_PROTOCOL_STAGE_B_ACTIVATION
input_base_commit: 783b03253536a0fdada54fc10571d503915f5c6a
review_target_commit: 783b03253536a0fdada54fc10571d503915f5c6a
applicable_review_id: ML-DEVOS-AS-110
```

This handoff is the bounded activation record. It is evidence, not authority. Routing, turn, scope and flags live only in `coordination/STATE.md`. The Builder does not self-approve, and every result below is `ACTOR_REPORTED`.

## Objective

Perform the RFC-020 §21 Stage B atomic activation authorized by `D-080`, as the one commit that publishes this handoff.

Provenance:
- **Base:** `783b03253536a0fdada54fc10571d503915f5c6a`, the published `ML-DEVOS-AS-110` transition (`TURN: PAULO`, `PROTOCOL_VERSION: 1`), from a fresh Context Bootstrap.
- **Activation commit:** its sole parent is the base.

The commit carries:
- `D-080`, recording Paulo's chat authorization verbatim;
- `PROTOCOL_VERSION: 1 -> 2`;
- the five V2 directive selector fields, with `CURRENT_DIRECTIVE: NONE` and empty values;
- the V2 status wording;
- this handoff and the STATE gate.

It routes to `TURN: ARCHITECT`, a non-Builder gate. It is published with `--protocol-cutover 1->2 --session-protocol 1`.

**Not done:**
- no real directive was issued, and `coordination/CURRENT_DIRECTIVE.md` stays a template;
- no checker or test changes;
- no product, admin, site, runtime, media, D1/R2, migration, deployment, S6/S7, V2A or Stage C work;
- the suspended D-068 local draft was not touched.

## Changed files

Diff against `783b03253536a0fdada54fc10571d503915f5c6a`:
- `brain/DECISION_LOG.md`: `D-080` appended.
- `coordination/STATE.md`: the V2 header and the Architect verification gate.
- `coordination/CURRENT_HANDOFF.md`: this file. The outgoing `H-RFC020-STAGE-A-REM1-0001` bytes were already archived by the AS-110 transition.
- Status wording only, "not active" → "active since D-080":
  - `brain/protocols/CONTEXT_BOOTSTRAP.md` (header and §10 title/lead);
  - `CLAUDE.md` (startup-set labels and the protocol line);
  - `AGENTS.md`;
  - `brain/00_HOME.md`;
  - `brain/protocols/ARCHITECT_SYNC.md`;
  - `coordination/README.md`;
  - `coordination/CURRENT_DIRECTIVE.md` (the status/lead of the inert template; the format reference is unchanged);
  - `coordination/archive/directives/README.md`.
- Canonical Skills `architect-review-sync`, `implementation-handoff` and `project-orientation-state-recovery` (one status line each), plus their `.claude/skills/**` bridge copies, regenerated by `node scripts/generate-claude-skills-bridge.mjs`.

## Tests and evidence

The results are recorded at publication and in the publishing session's report. The positive gate is the exit code of the check-only and real publish runs with `--protocol-cutover 1->2 --session-protocol 1`, followed by a fresh `--commit <tip> --session-protocol 2` bootstrap.

## Unresolved findings and limitations

- **Code comment:** the header comment of `scripts/check-context-bootstrap.mjs` (lines 10–16) still describes V2 as "NOT active". It was left unchanged because checker edits fall outside `D-080`'s state/evidence scope. It is a comment only, not behaviour.
- **Historical records** (`ML-DEVOS-RFC-020` status wording, archived reviews and handoffs) describe V2 as inactive at their time. They are unchanged by design.
- **Obligations:** every `coordination/OPERATIVE_OBLIGATIONS.md` row is carried forward unchanged.
- **Traceability:** the existing debt (`CORE-022`, `WEB-REQ-009`, DRIFT) is unchanged and not regenerated.
- **Unverified:** V2 activation is not verified until the Architect's independent review.

## Evidence locations

- The commit diff against `783b03253536a0fdada54fc10571d503915f5c6a`.
- `coordination/STATE.md` header.
- `brain/DECISION_LOG.md` `D-080`.

## Governing references

- **Authority:** `D-080`.
- **Review:** `ML-DEVOS-AS-110` (controlling).
- **Design:** `ML-DEVOS-RFC-020` §5, §21, §22.
- **Protocol:** `brain/protocols/CONTEXT_BOOTSTRAP.md` §10.
- **Obligations:** `coordination/OPERATIVE_OBLIGATIONS.md`.

## Next action

The Architect independently verifies the activation under the next unused immutable Architect Sync ID after `ML-DEVOS-AS-110`. No Builder action is authorized.
