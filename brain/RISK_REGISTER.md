# Risk Register

Seeded from `docs/MAISOGLABS_WEBSITE_GOVERNANCE_ADMIN_PLAN_v0.1.txt` §14. Traceability: `Risk → Control → Test → Evidence → Status`.

Status values used here: `NOT STARTED` (no control/mitigation work has begun — typically because the risk's subject system doesn't exist yet), `NOT YET APPLICABLE` (the risk cannot materialize yet because its preconditions don't exist, e.g. no admin surface to bypass), `OPEN` (the risk's subject system exists and the risk is not yet mitigated/verified), `MITIGATED` (a control exists and is evidenced), `VERIFIED` (independently reproduced/inspected evidence supports the mitigation).

A future risk being `NOT YET APPLICABLE` is never recorded as resolved or mitigated — it becomes `OPEN` the moment the relevant feature (e.g. admin) is designed, and must be tracked from there.

| ID | Risk | Control | Test | Evidence | Status |
|---|---|---|---|---|---|
| RISK-WEB-001 | Production outage after deployment | Not designed | None | None | `NOT STARTED` |
| RISK-WEB-002 | Admin authorization bypass | `WEB-INC-001`: `worker/auth.mjs` fail-closed server-side Cloudflare Access JWT verification for `/admin`/`/admin/*`; rejects missing/malformed/expired/wrong-audience/wrong-issuer/untrusted-key assertions before any asset is served (critical invariant: no valid server-verified identity → no admin asset, `ML-DEVOS-AS-011` `AS11-F002`). Remediation Cycle 1 (`ML-DEVOS-AS-012`) closes two further bypass classes: (`AS12-F001`) the auth configuration itself (team domain/audience) is validated and fails closed on missing/blank/placeholder/malformed values, with no JWKS/network lookup attempted when invalid; (`AS12-F003`) `wrangler.jsonc`'s `assets.html_handling` is pinned to `auto-trailing-slash` so alternate static URL forms (`/admin.html`, `/admin/index.html`) cannot bypass the protected path. `WEB-INC-002` (`ML-DEVOS-RFC-004`/`ML-DEVOS-AS-015`/`D-025`) extends the same fail-closed boundary to the new `GET /admin/api/dashboard` endpoint: the dashboard/D1-reading `dispatch` callback runs only after authentication succeeds (never before), so the same bypass classes above (missing/malformed/expired/wrong-audience/wrong-issuer/untrusted-key/bad-config) are proven to prevent dashboard-handler/D1 invocation, not merely asset serving. `WEB-INC-003` (`ML-DEVOS-RFC-006`/`ML-DEVOS-AS-020`/`D-027`) extends the same boundary again to the five new project mutation/preview routes, and additionally gates mutation (not preview) on a bounded non-empty verified Access `sub`: a valid Access assertion whose `sub` is empty/absent (the service-token-style shape) is authenticated but not mutation-eligible and is rejected with `403` before any D1 access | `tests/worker-auth.test.mjs` (30 tests, 14 for config fail-closed); `tests/worker-admin-dashboard.test.mjs` (3 tests proving zero D1 invocation on unauthenticated/malformed/wrong-audience/invalid-config requests to the dashboard endpoint); `tests/worker-admin-projects.test.mjs` (auth-negative tests for all 5 new routes proving zero D1 invocation on missing/malformed auth, plus a dedicated empty-`sub` test per mutating route proving `403`/zero D1 invocation); local `wrangler dev` smoke tests: `GET /admin` → 401 without/with garbage token; `GET /admin.html` → 307 redirect (empty body) → 401 on follow; `GET /admin/index.html` → 401; `GET /admin/api/dashboard` → 401 (no token); `POST /admin/api/projects`, `GET .../preview`, `POST .../publish`, `DELETE /admin/api/projects` (no token) → 401 | Implementer-reported: 152/152 passing (full suite); local dev smoke tests | `MITIGATED` at the repository-implementation level — not `VERIFIED` (no Architect-reproduced or production/runtime evidence); no longer `NOT YET APPLICABLE` now that an admin surface exists, per the register's own rule below |
| RISK-WEB-003 | Content/data loss | Git history is the only current rollback mechanism (`docs/CHANGE_LEDGER.md` records prior rollback SHAs) | None automated | `docs/CHANGE_LEDGER.md` | `OPEN` — a real, already-applicable risk today since content lives only in Git, with no automated backup/rollback tooling |
| RISK-WEB-004 | Secret exposure | No secrets exist in source; `.gitignore` excludes env/log files; no `process.env` usage found. `WEB-INC-001` introduces the first server-side config surface (`wrangler.jsonc` `vars`) but uses placeholder strings only (`ACCESS_TEAM_DOMAIN`/`ACCESS_AUD`), never a real value or secret. `WEB-INC-005`'s `d1_databases` binding has no `database_id` and `remote: false`, so there is no real/production D1 identifier to leak either. `WEB-INC-004`'s `r2_buckets` binding (`MEDIA`) is likewise `remote: false` with only a local-only bucket name (`maisog-labs-web-inc-004-local`) and no credential/custom-domain/production identifier — real values (for any of these surfaces) are set outside tracked source | Manual grep, re-run this cycle over `worker/ migrations/ scripts/ tests/worker-auth.test.mjs tests/d1-migration.test.mjs tests/worker-admin-media.test.mjs wrangler.jsonc`: no `process.env`, no private-key/PEM markers, no secret/token/credential keyword matches beyond explanatory comments, no `.env*` files found | This handoff's evidence log | `MITIGATED` for the current surface (including `WEB-INC-001`'s, `WEB-INC-005`'s, and `WEB-INC-004`'s new files); will need re-evaluation the moment a real Cloudflare Access application/secret, a real D1 database, or a real R2 bucket is provisioned |
| RISK-WEB-005 | Mobile UI regression | `docs/CHANGE_LEDGER.md` records manual desktop/mobile QA as part of past releases | No automated mobile/visual test exists | `docs/CHANGE_LEDGER.md` (manual QA notes only) | `OPEN` — no automated coverage |
| RISK-WEB-006 | Broken production routing | Two static routes (`/`, `/admin`) plus Next's generated `/_not-found`; `wrangler.jsonc` sets `not_found_handling: "404-page"`, explicitly pinned `assets.html_handling: "auto-trailing-slash"` (`ML-DEVOS-AS-012` `AS12-F003` — no longer an implicit default), and selective `assets.run_worker_first: ["/admin", "/admin/*"]` (`WEB-INC-001`) so only those two path patterns reach the Worker — every other route stays asset-first, unchanged | `npm run build` succeeds and emits `out/404.html`, `out/index.html`, `out/admin.html`; `npx wrangler deploy --dry-run` confirms the config parses and bundles with no external Cloudflare resource touched; local `wrangler dev`: `GET /` → 200, `GET /admin` → 401 (no token), `GET /admin/` → 401, `GET /nope` → 404, `GET /admin.html` → 307 redirect to `/admin` with an empty body (no content leak) → 401 on follow, `GET /admin/index.html` → 401 | This cycle's `npm run build`/`wrangler deploy --dry-run`/`wrangler dev` runs (see `TEST_LEDGER.md`) | `IMPLEMENTED`, not independently (Architect) verified yet |
| RISK-WEB-007 | Admin changes fail to persist | `WEB-INC-003` (`ML-DEVOS-RFC-006`/`ML-DEVOS-AS-020`/`D-027`) implements the first admin write path against `WEB-INC-005`'s local D1 substrate: create-draft, edit-draft, publish, and unpublish. Every successful state-changing mutation and its `audit_log` success row commit as one `db.batch()` transaction (`worker/d1/projects.mjs`'s `buildCreateDraftBatch`/`buildEditDraftBatch`/`buildPublishBatch`/`buildUnpublishBatch`); a storage/transaction failure leaves no partial project/revision row and never reports success (`worker/admin/projects.mjs`'s generic-500 catch paths). Publish additionally re-reads and fully revalidates the persisted draft before promoting it, refusing to promote content that could not have passed this server's own write-time validation | `tests/worker-admin-projects.test.mjs`: real-D1 test forcing a genuine SQL-level audit-statement failure inside a create-draft-shaped batch and proving the project/revision insert rolls back with it; simulated-storage-failure test proving a failed publish changes nothing and returns a generic `500`; "publish fully revalidates ... and refuses to promote corrupted stored content"; create/edit/publish/unpublish success tests each asserting exactly the expected row/pointer/audit state | Implementer-reported: 152/152 passing (full suite) | `MITIGATED` at the repository-implementation level — not `VERIFIED` (no Architect-reproduced or production/runtime evidence); no longer `NOT YET APPLICABLE` now that a real admin write path exists, per the register's own rule below |
| RISK-WEB-008 | Invalid content breaks rendering | `lib/content/schema.mjs` rejects invalid/unknown content at build time; `projectPublishedContent` throws before render | `tests/content.test.mjs` — 22 "rejects ..." cases plus the unpublished-root test | Implementer-reported: 27/27 passing (Phase 0 + Phase 1 re-run) | `IMPLEMENTED`, implementer-reported evidence only |
| RISK-WEB-009 | Development/production behavior diverges | Static export means dev (`next dev`) and prod (`next build` + Worker) share the same content pipeline, but dev is not statically exported | No automated parity test | None beyond code inspection | `OPEN` |
| RISK-WEB-010 | Deployment succeeds but UI is materially broken | No automated post-deploy check exists | None | `docs/CHANGE_LEDGER.md` notes past manual verification only | `OPEN` |
| RISK-WEB-011 | Unsafe design input introduces XSS/code execution | `lib/content/schema.mjs`'s `text()` validator rejects control characters and angle brackets; `href()` allowlists in-page anchors and validated `mailto:` only. `WEB-INC-003` (`ML-DEVOS-RFC-006`/`ML-DEVOS-AS-020`/`D-027`) is the first admin mutation input surface, and every project field is validated server-side through the existing, unmodified `worker/d1/validate.mjs` (`validateProjectRevisionContent`, `validateProjectId`, `validateProjectSlug`) before any write — bounded length/enum/pattern checks, unknown-field rejection, no free-form HTML/script field of any kind. Client-supplied values are never trusted as authority | `tests/content.test.mjs`: "rejects HTML", "rejects javascript link", "rejects protocol-relative link", "rejects email header injection"; `tests/worker-admin-projects.test.mjs`: "create-draft rejects invalid content with 400, writes no project/revision row..." | Implementer-reported test pass | `IMPLEMENTED`/`MITIGATED` for both the content-editing surface and the new project mutation input surface; not `VERIFIED` (no independent security review); no longer scoped to "no admin input surface exists yet" now that `WEB-INC-003` introduces one |
| RISK-WEB-012 | Media upload abuse | `WEB-INC-004` (`ML-DEVOS-RFC-007`/`ML-DEVOS-AS-023`/`D-029`) implements the first (and only) upload path, `POST /admin/api/media`: authenticated + bounded-subject + same-origin before any validation; content type restricted to an allowlist (`image/jpeg`/`image/png`/`image/webp`), SVG explicitly excluded; the declared `Content-Type` must match the body's own bounded magic-byte signature (`worker/media/signature.mjs`) rather than being trusted alone; a real 5 MiB byte limit is enforced via a byte-accurate streaming reader with an early `Content-Length` reject, mirroring the existing AS21-F009 technique but implemented separately (`worker/admin/media.mjs`); the media id and R2 storage key are always server-generated, never derived from client input (no path traversal / filename injection surface); every upload's D1 write commits atomically with a `media_upload` audit row. Local R2 only — `wrangler.jsonc`'s `MEDIA` binding is `remote: false`, no public bucket/custom domain | `tests/worker-admin-media.test.mjs` (22 tests): auth/subject/origin boundary with zero D1/R2 invocation; JPEG/PNG/WebP acceptance with real object+row+audit atomicity; SVG rejection (415); signature/Content-Type mismatch rejection (400); non-image-disguised-as-allowed-type rejection (400); zero-byte and over-5-MiB rejection (400/413, including a declared-Content-Length early reject); missing-alt-text rejection (400); R2-write-failure-leaves-no-D1-row; D1-batch-failure-after-R2-success triggers a compensating delete (object actually removed, no media row survives); compensating-delete-failure still fails the request without crashing; positive-projection list (no storage key/uploader identity exposed); protected 404/405 for any route outside the exact two authorized ones. `tests/worker-admin-projects.test.mjs` adds "referenced media must exist and be active" / duplicate-role rejection for the project-media-snapshot integration surface | Implementer-reported: 195/195 passing (full suite); local `wrangler d1 migrations apply --local` (17 tables) and `wrangler dev --local` unauthenticated-fail-closed smoke test — see `TEST_LEDGER.md`/`coordination/IMPLEMENTER_HANDOFF.md` | `MITIGATED` at the repository-implementation level — not `VERIFIED` (no Architect-reproduced or production/runtime evidence); no longer `NOT YET APPLICABLE` now that a real upload path exists, per the register's own rule below. The one accepted operational limitation (an orphaned local R2 object if a D1 batch failure's own compensating delete also fails — AS23-F009) is documented, not silently treated as resolved |
| RISK-WEB-013 | Draft/private content becomes public | Draft/archived records are filtered from the build-time public projection (`lib/content/public.mjs`) — the actual public site never reads draft content at all. As of `WEB-INC-002` (`ML-DEVOS-RFC-004`/`ML-DEVOS-AS-015`/`D-025`), the one HTTP route that can reach draft pointer content (`GET /admin/api/dashboard`, authenticated only) never returns raw draft content: the allowlist serializer reduces a draft revision to a derived lifecycle state (`draft`/`published_with_draft`) and a bounded display label only — full body/about/contact/SEO/hero copy, summaries, stacks, emails, and provenance are never included regardless of publish state (`ML-DEVOS-AS-015` `AS15-F004`). `WEB-INC-003` (`ML-DEVOS-RFC-006`/`ML-DEVOS-AS-020`/`D-027`) adds a second authenticated route that does deliberately return full draft content, `GET /admin/api/projects/:id/preview` — it is authenticated (same fail-closed boundary), reads only the entity's exact `draft_revision_id`, is never public, and this is its designed purpose (previewing a draft before publish), not a leak (`AS20-F014`). No unauthenticated path reaches draft content at all | `tests/content.test.mjs`: "all record collections filter drafts and archives before serialization"; `tests/d1-migration.test.mjs`: "published/draft/archived source fixtures map to the correct pointer state"; `tests/worker-admin-dashboard.test.mjs`: "the dashboard response excludes every non-allowlisted field, including sensitive legacy content" (proves draft-sourced sensitive fields never leak even through the one authenticated route that touches drafts); `tests/worker-admin-projects.test.mjs`: "unauthenticated GET .../preview is rejected with zero D1 invocation"; "GET .../preview returns 404 when no draft exists and never falls back to published content" | Implementer-reported test pass. **Important caveat, carried from `docs/CONTENT.md`:** this filtering is not confidentiality — the Git source itself, including drafts, remains public in this public repository; this now also applies to any draft content migrated into the local D1 database file, which is untracked/gitignored (`.wrangler/`) but not encrypted or access-controlled. | `OPEN` despite the passing tests — the residual risk (public Git source) is architectural, not something the current tests can close |
| RISK-WEB-014 | Admin actions are not auditable | `WEB-INC-008` (`ML-DEVOS-RFC-005`/`ML-DEVOS-AS-017`/`D-026`) implemented the append-only `audit_log` substrate itself. `WEB-INC-003` (`ML-DEVOS-RFC-006`/`ML-DEVOS-AS-020`/`D-027`) is the first real admin action to call it: every successful create-draft/edit-draft/publish/unpublish appends exactly one `result: success` `audit_log` row (`project_create_draft`/`project_update_draft`/`project_publish`/`project_unpublish`, entity type `project`) atomically with the business state change, and bounded authenticated failures (validation/not-found/conflict/stale/revalidation) append a `result: failure` row when a safe entity reference exists. Audit actor is a bounded opaque value derived only from the verified Access `sub` (`cf-access:<sub>`), never the full JWT/email/claims | `tests/worker-admin-projects.test.mjs`: every create/edit/publish/unpublish success test asserts exactly one matching audit row with the correct action/entity/revision/result/actor; every failure-path test (duplicate id/slug, invalid content, not-found, stale pointer, publish revalidation failure) asserts a `result: failure` row; the atomicity test proves a forced audit-statement failure rolls back the paired business mutation, so a false-success audit row can never be recorded; `tests/d1-audit.test.mjs` (17 tests, unchanged) still covers the substrate itself in isolation | Implementer-reported: 152/152 passing (full suite) | `MITIGATED` at the repository-implementation level for the exact `WEB-INC-003` mutation set — not `VERIFIED` (no Architect-reproduced or production/runtime evidence); no longer `NOT YET APPLICABLE` now that a real admin mutation/action surface exists, per the register's own rule below. Every other admin action (media, journal, theme/design, project delete) remains `NOT YET APPLICABLE` until its own increment exists |
| RISK-WEB-015 | Static-to-database migration loses existing content | `WEB-INC-005`'s deterministic migration (`worker/d1/migrate.mjs`) reuses `lib/content/schema.mjs`'s `validateContent()` as its first validation gate, then maps every current domain (including `services`, not currently rendered) into the D1 revision substrate with an explicit published/draft/archived pointer mapping; a second run is a verified no-op, and a changed-content rerun is an explicit refusal with no partial write | `tests/d1-migration.test.mjs`: "fresh migration ... achieves deep parity with `projectPublishedContent(siteContent)`" (full deep-equal, all domains); "second migration run ... is a deterministic no-op"; "migration refuses to overwrite an entity whose stored content differs ..." | Implementer-reported: parity/repeat-run/refusal tests pass; this is a local-only migration into a parallel substrate — no cutover of the actual public source has occurred, so this migration is not yet the "the" migration this risk describes for the public system, only its proven mechanism | `MITIGATED` for the local-only D1 revision substrate migrated this cycle — `VERIFIED` requires independent Architect reproduction; a future public cutover migration remains its own separately authorized/tested event |

## Register maintenance rule

Do not close or mark `MITIGATED`/`VERIFIED` any risk whose control has not been evidenced in this table or a linked test. When a future phase introduces the admin/auth/storage surface these `NOT YET APPLICABLE` risks anticipate, flip them to `OPEN` in the same commit that introduces the surface, not after.
