# Risk Register

Seeded from `docs/MAISOGLABS_WEBSITE_GOVERNANCE_ADMIN_PLAN_v0.1.txt` §14. Traceability: `Risk → Control → Test → Evidence → Status`.

Status values used here: `NOT STARTED` (no control/mitigation work has begun — typically because the risk's subject system doesn't exist yet), `NOT YET APPLICABLE` (the risk cannot materialize yet because its preconditions don't exist, e.g. no admin surface to bypass), `OPEN` (the risk's subject system exists and the risk is not yet mitigated/verified), `MITIGATED` (a control exists and is evidenced), `VERIFIED` (independently reproduced/inspected evidence supports the mitigation).

A future risk being `NOT YET APPLICABLE` is never recorded as resolved or mitigated — it becomes `OPEN` the moment the relevant feature (e.g. admin) is designed, and must be tracked from there.

| ID | Risk | Control | Test | Evidence | Status |
|---|---|---|---|---|---|
| RISK-WEB-001 | Production outage after deployment | Not designed | None | None | `NOT STARTED` |
| RISK-WEB-002 | Admin authorization bypass | `WEB-INC-001`: `worker/auth.mjs` fail-closed server-side Cloudflare Access JWT verification for `/admin`/`/admin/*`; rejects missing/malformed/expired/wrong-audience/wrong-issuer/untrusted-key assertions before any asset is served (critical invariant: no valid server-verified identity → no admin asset, `ML-DEVOS-AS-011` `AS11-F002`). Remediation Cycle 1 (`ML-DEVOS-AS-012`) closes two further bypass classes: (`AS12-F001`) the auth configuration itself (team domain/audience) is validated and fails closed on missing/blank/placeholder/malformed values, with no JWKS/network lookup attempted when invalid; (`AS12-F003`) `wrangler.jsonc`'s `assets.html_handling` is pinned to `auto-trailing-slash` so alternate static URL forms (`/admin.html`, `/admin/index.html`) cannot bypass the protected path. `WEB-INC-002` (`ML-DEVOS-RFC-004`/`ML-DEVOS-AS-015`/`D-025`) extends the same fail-closed boundary to the new `GET /admin/api/dashboard` endpoint: the dashboard/D1-reading `dispatch` callback runs only after authentication succeeds (never before), so the same bypass classes above (missing/malformed/expired/wrong-audience/wrong-issuer/untrusted-key/bad-config) are proven to prevent dashboard-handler/D1 invocation, not merely asset serving | `tests/worker-auth.test.mjs` (30 tests, 14 for config fail-closed); `tests/worker-admin-dashboard.test.mjs` (3 tests proving zero D1 invocation on unauthenticated/malformed/wrong-audience/invalid-config requests to the dashboard endpoint); local `wrangler dev` smoke tests: `GET /admin` → 401 without/with garbage token; `GET /admin.html` → 307 redirect (empty body) → 401 on follow; `GET /admin/index.html` → 401; `GET /admin/api/dashboard` → 401 (no token) | Implementer-reported: 96/96 passing (full suite); local dev smoke tests | `MITIGATED` at the repository-implementation level — not `VERIFIED` (no Architect-reproduced or production/runtime evidence); no longer `NOT YET APPLICABLE` now that an admin surface exists, per the register's own rule below |
| RISK-WEB-003 | Content/data loss | Git history is the only current rollback mechanism (`docs/CHANGE_LEDGER.md` records prior rollback SHAs) | None automated | `docs/CHANGE_LEDGER.md` | `OPEN` — a real, already-applicable risk today since content lives only in Git, with no automated backup/rollback tooling |
| RISK-WEB-004 | Secret exposure | No secrets exist in source; `.gitignore` excludes env/log files; no `process.env` usage found. `WEB-INC-001` introduces the first server-side config surface (`wrangler.jsonc` `vars`) but uses placeholder strings only (`ACCESS_TEAM_DOMAIN`/`ACCESS_AUD`), never a real value or secret. `WEB-INC-005`'s `d1_databases` binding has no `database_id` and `remote: false`, so there is no real/production D1 identifier to leak either — real values (for either surface) are set outside tracked source | Manual grep, re-run this cycle over `worker/ migrations/ scripts/ tests/worker-auth.test.mjs tests/d1-migration.test.mjs wrangler.jsonc`: no `process.env`, no private-key/PEM markers, no secret/token/credential keyword matches beyond explanatory comments, no `.env*` files found | This handoff's evidence log | `MITIGATED` for the current surface (including `WEB-INC-001`'s and `WEB-INC-005`'s new files); will need re-evaluation the moment a real Cloudflare Access application/secret or a real D1 database is provisioned |
| RISK-WEB-005 | Mobile UI regression | `docs/CHANGE_LEDGER.md` records manual desktop/mobile QA as part of past releases | No automated mobile/visual test exists | `docs/CHANGE_LEDGER.md` (manual QA notes only) | `OPEN` — no automated coverage |
| RISK-WEB-006 | Broken production routing | Two static routes (`/`, `/admin`) plus Next's generated `/_not-found`; `wrangler.jsonc` sets `not_found_handling: "404-page"`, explicitly pinned `assets.html_handling: "auto-trailing-slash"` (`ML-DEVOS-AS-012` `AS12-F003` — no longer an implicit default), and selective `assets.run_worker_first: ["/admin", "/admin/*"]` (`WEB-INC-001`) so only those two path patterns reach the Worker — every other route stays asset-first, unchanged | `npm run build` succeeds and emits `out/404.html`, `out/index.html`, `out/admin.html`; `npx wrangler deploy --dry-run` confirms the config parses and bundles with no external Cloudflare resource touched; local `wrangler dev`: `GET /` → 200, `GET /admin` → 401 (no token), `GET /admin/` → 401, `GET /nope` → 404, `GET /admin.html` → 307 redirect to `/admin` with an empty body (no content leak) → 401 on follow, `GET /admin/index.html` → 401 | This cycle's `npm run build`/`wrangler deploy --dry-run`/`wrangler dev` runs (see `TEST_LEDGER.md`) | `IMPLEMENTED`, not independently (Architect) verified yet |
| RISK-WEB-007 | Admin changes fail to persist | Not designed (no admin write/edit/mutation *path* exists. `WEB-INC-005` implemented a local-only D1 persistence *substrate*; `WEB-INC-002` (`ML-DEVOS-RFC-004`/`ML-DEVOS-AS-015`/`D-025`) connected it to exactly one **read-only** dashboard endpoint (`GET /admin/api/dashboard`) — no admin code writes to it: there is still no create/edit/publish handler of any kind for this risk to apply to; `MUTATION_AUTHORIZED: NO`) | None | None | `NOT YET APPLICABLE` — a read-only dashboard existing is not the same as an admin write path existing to fail |
| RISK-WEB-008 | Invalid content breaks rendering | `lib/content/schema.mjs` rejects invalid/unknown content at build time; `projectPublishedContent` throws before render | `tests/content.test.mjs` — 22 "rejects ..." cases plus the unpublished-root test | Implementer-reported: 27/27 passing (Phase 0 + Phase 1 re-run) | `IMPLEMENTED`, implementer-reported evidence only |
| RISK-WEB-009 | Development/production behavior diverges | Static export means dev (`next dev`) and prod (`next build` + Worker) share the same content pipeline, but dev is not statically exported | No automated parity test | None beyond code inspection | `OPEN` |
| RISK-WEB-010 | Deployment succeeds but UI is materially broken | No automated post-deploy check exists | None | `docs/CHANGE_LEDGER.md` notes past manual verification only | `OPEN` |
| RISK-WEB-011 | Unsafe design input introduces XSS/code execution | `lib/content/schema.mjs`'s `text()` validator rejects control characters and angle brackets; `href()` allowlists in-page anchors and validated `mailto:` only | `tests/content.test.mjs`: "rejects HTML", "rejects javascript link", "rejects protocol-relative link", "rejects email header injection" | Implementer-reported test pass | `IMPLEMENTED` for the current content-editing surface (no admin write/edit/mutation input surface exists yet to introduce a new injection surface — `WEB-INC-001`'s `/admin` accepts no input at all, only a Cloudflare Access assertion header that is cryptographically verified, not parsed as content) |
| RISK-WEB-012 | Media upload abuse | Not designed (no upload path exists) | None | None | `NOT YET APPLICABLE` |
| RISK-WEB-013 | Draft/private content becomes public | Draft/archived records are filtered from the build-time public projection (`lib/content/public.mjs`) — the actual public site never reads draft content at all. As of `WEB-INC-002` (`ML-DEVOS-RFC-004`/`ML-DEVOS-AS-015`/`D-025`), the one HTTP route that can reach draft pointer content (`GET /admin/api/dashboard`, authenticated only) never returns raw draft content: the allowlist serializer reduces a draft revision to a derived lifecycle state (`draft`/`published_with_draft`) and a bounded display label only — full body/about/contact/SEO/hero copy, summaries, stacks, emails, and provenance are never included regardless of publish state (`ML-DEVOS-AS-015` `AS15-F004`). No unauthenticated path reaches draft content at all | `tests/content.test.mjs`: "all record collections filter drafts and archives before serialization"; `tests/d1-migration.test.mjs`: "published/draft/archived source fixtures map to the correct pointer state"; `tests/worker-admin-dashboard.test.mjs`: "the dashboard response excludes every non-allowlisted field, including sensitive legacy content" (proves draft-sourced sensitive fields never leak even through the one authenticated route that touches drafts) | Implementer-reported test pass. **Important caveat, carried from `docs/CONTENT.md`:** this filtering is not confidentiality — the Git source itself, including drafts, remains public in this public repository; this now also applies to any draft content migrated into the local D1 database file, which is untracked/gitignored (`.wrangler/`) but not encrypted or access-controlled. | `OPEN` despite the passing tests — the residual risk (public Git source) is architectural, not something the current tests can close |
| RISK-WEB-014 | Admin actions are not auditable | `WEB-INC-008` (`ML-DEVOS-RFC-005`/`ML-DEVOS-AS-017`/`D-026`) implements the append-only `audit_log` substrate itself — `migrations/0002_web_inc_008_audit_log.sql` (schema + `BEFORE UPDATE`/`BEFORE DELETE` triggers that unconditionally `RAISE(ABORT, ...)`) and `worker/d1/audit.mjs` (`validateAuditEvent`/`appendAuditEvent`: strict field allowlist, server-generated `occurred_at`, fixed parameterized INSERT only, storage failure propagates by throwing and is never reported as success). No admin mutation/action surface exists yet — `WEB-INC-002` remains read-only (no create/edit/publish/unpublish/delete/upload of any kind), `MUTATION_AUTHORIZED: NO` — so no real admin action has ever produced an `audit_log` row; this increment proves only that the substrate itself works in isolation. Proving a real mutation calls it is `WEB-INC-003`'s acceptance criterion, not this one's | `tests/d1-audit.test.mjs` (16 tests: exact 15-table inventory, valid success/failure append, unknown-field/invalid-actor/invalid-action-or-entityType/invalid-entityId/invalid-revisionId/invalid-result rejection each writing zero rows, JWT-shaped-actor rejection, DB-level UPDATE/DELETE rejection, forced-INSERT-failure propagation, no-foreign-key/logical-reference proof, dashboard-payload-unchanged/no-audit-leak proof) | Implementer-reported: 112/112 passing (full suite, includes the 96 pre-existing plus these 16); `wrangler d1 migrations apply DB --local` applied `0001` then `0002` cleanly; direct `wrangler d1 execute DB --local` confirmed exactly 15 tables and that a direct `UPDATE`/`DELETE` against `audit_log` is rejected with `SQLITE_CONSTRAINT_TRIGGER` | `NOT YET APPLICABLE` — the substrate existing is not the same as an admin mutation/action surface existing to audit; flips to `OPEN` only when `WEB-INC-003` (or any other mutation increment) introduces a real action for this substrate to record |
| RISK-WEB-015 | Static-to-database migration loses existing content | `WEB-INC-005`'s deterministic migration (`worker/d1/migrate.mjs`) reuses `lib/content/schema.mjs`'s `validateContent()` as its first validation gate, then maps every current domain (including `services`, not currently rendered) into the D1 revision substrate with an explicit published/draft/archived pointer mapping; a second run is a verified no-op, and a changed-content rerun is an explicit refusal with no partial write | `tests/d1-migration.test.mjs`: "fresh migration ... achieves deep parity with `projectPublishedContent(siteContent)`" (full deep-equal, all domains); "second migration run ... is a deterministic no-op"; "migration refuses to overwrite an entity whose stored content differs ..." | Implementer-reported: parity/repeat-run/refusal tests pass; this is a local-only migration into a parallel substrate — no cutover of the actual public source has occurred, so this migration is not yet the "the" migration this risk describes for the public system, only its proven mechanism | `MITIGATED` for the local-only D1 revision substrate migrated this cycle — `VERIFIED` requires independent Architect reproduction; a future public cutover migration remains its own separately authorized/tested event |

## Register maintenance rule

Do not close or mark `MITIGATED`/`VERIFIED` any risk whose control has not been evidenced in this table or a linked test. When a future phase introduces the admin/auth/storage surface these `NOT YET APPLICABLE` risks anticipate, flip them to `OPEN` in the same commit that introduces the surface, not after.
