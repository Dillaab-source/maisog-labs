# Implementation Status

Status vocabulary: `NOT STARTED` · `IN PROGRESS` · `IMPLEMENTED` · `VERIFIED` · `BLOCKED` · `DEFERRED`.

Code existence alone does not justify `VERIFIED`; see `GOVERNANCE_MAP.md`.

| Area | Status | Evidence |
|---|---|---|
| Governance bootstrap (this document set) | `IMPLEMENTED` | This commit: `brain/*`, `AGENTS.md`/`README.md` deployment-wording fix. Not yet Architect-reviewed at time of writing. |
| Public homepage rendering | `IMPLEMENTED` | `app/page.js`, `app/layout.js`; builds successfully (`npm run build`, Phase 0 + Phase 1 re-run — see `TEST_LEDGER.md`). |
| Content contract / schema validation | `IMPLEMENTED` | `lib/content/schema.mjs`, `lib/content/public.mjs`, `lib/content/local.mjs`; `tests/content.test.mjs` (27/27 passing, implementer-reported). |
| Projects feature (public site) | `IMPLEMENTED` (presentation only) | `components/ProjectRail.js`, `data/site.js` project records. Public rendering still reads only `data/site.js`; a local D1 admin write path now exists (see "Project mutation capability" below) but does not affect the public site (`D1 PUBLISHED ≠ PRODUCTION WEBSITE LIVE`, `AS20-F015`) — public content changes still require a direct source edit and redeploy. |
| Journal feature | `NOT STARTED` | No schema field, data, route, or component exists (confirmed by repository-wide search in Phase 0). |
| Admin portal (`/admin`) | `IMPLEMENTED` (read-only status dashboard + project mutation API) — admin UI for project mutation `NOT STARTED` | `WEB-INC-001` (`ML-DEVOS-RFC-002`/`ML-DEVOS-AS-011`/`D-023`) established the auth-only placeholder; `WEB-INC-002` (`ML-DEVOS-RFC-004`/`ML-DEVOS-AS-015`/`D-025`) upgraded it into a read-only status dashboard (`app/admin/page.js`, `app/admin/DashboardClient.js`); `WEB-INC-003` (`ML-DEVOS-RFC-006`/`ML-DEVOS-AS-020`/`D-027`) adds a bounded project mutation API (`worker/admin/projects.mjs`) behind the same auth boundary, but no UI controls for it exist yet — `app/admin/DashboardClient.js` is unmodified and still renders read-only status only. `AUTH-ONLY/READ-ONLY ADMIN SURFACE EXISTS ≠ ADMIN DESIGN-CONTROL/EDITING/MUTATION CAPABILITY EXISTS` (`ML-DEVOS-AS-013`, `ML-DEVOS-AS-015`) no longer describes the API layer, but still describes the UI layer. |
| Admin dashboard read endpoint (`GET /admin/api/dashboard`) | `IMPLEMENTED` (local-only, read-only) | `WEB-INC-002` (`ML-DEVOS-RFC-004`/`ML-DEVOS-AS-015`/`D-025`): `worker/admin/dashboard.mjs` reads the `WEB-INC-005` local D1 substrate via `worker/d1/repository.mjs` only after WEB-INC-001 authentication succeeds; returns an explicit allowlisted projection; non-GET methods and unknown `/admin/api/*` paths are rejected before any D1 call; `Cache-Control: no-store` on every protected response. `tests/worker-admin-dashboard.test.mjs` (20/20 passing, implementer-reported). Unchanged and still passing after `WEB-INC-003` — proven to expose no project-mutation/audit data. |
| Project mutation capability | `IMPLEMENTED` (local-only) | `WEB-INC-003` (`ML-DEVOS-RFC-006`/`ML-DEVOS-AS-020`/`D-027`): `worker/admin/projects.mjs` + `worker/d1/projects.mjs` expose exactly `POST /admin/api/projects`, `PUT /admin/api/projects/:id/draft`, `GET /admin/api/projects/:id/preview`, `POST /admin/api/projects/:id/publish`, `POST /admin/api/projects/:id/unpublish` — no delete, no generic mutation API. Mutation requires a bounded non-empty verified Access `sub` (a service-token-style empty-sub assertion is rejected `403`); every mutating route enforces same-origin/JSON/bounded-body request hardening; create/edit each write one new immutable `project_revisions` row (never `UPDATE`d in place); slug is immutable; existing-project mutations require `expectedPublishedRevisionId`/`expectedDraftRevisionId` and fail `409` on stale state; publish fully re-reads and revalidates the persisted draft before promotion; every successful mutation and its `audit_log` success row commit as one `db.batch()` transaction (proven by a real-D1 test forcing the audit statement to fail and confirming the paired insert rolls back); bounded authenticated failures record a `result: failure` audit row. `tests/worker-admin-projects.test.mjs` (51/51 final remediation suite, implementer-reported; exact remediation source independently inspected). Final full suite reported 164/164. Commit-time stale-state guards, bounded mutation identity, UTF-8 byte-accurate body limits, and route/DB ordering were accepted by `ML-DEVOS-AS-022`. Schema unchanged — still exactly 15 tables; `migrations/0001_web_inc_005_init.sql`/`migrations/0002_web_inc_008_audit_log.sql` untouched. Implementation authority resets to `NO` on cycle closure; the capability remains implemented but does not carry authority into a new increment. |
| Authentication | `IMPLEMENTED` (repository level; not production-verified) | `WEB-INC-001`: `worker/index.mjs`/`worker/auth.mjs` fail-closed-verify a Cloudflare Access JWT assertion (via `jose`) for `/admin`/`/admin/*` only; `tests/worker-auth.test.mjs` (30/30 passing, implementer-reported). No production Cloudflare Access application exists yet, so this is not production/runtime-verified. No session/cookie mechanism and no editorial/mutation capability exist. |
| Persistent storage — D1 current-content revision substrate | `IMPLEMENTED` (local-only) | `WEB-INC-005` (`ML-DEVOS-RFC-003`/`ML-DEVOS-AS-013`/`D-024`), remediated per `ML-DEVOS-AS-014` Remediation Cycle 1: `migrations/0001_web_inc_005_init.sql` (exactly 14 tables), `worker/d1/{schema,validate,migrate,repository}.mjs`, `scripts/d1-migrate.mjs`; `tests/d1-migration.test.mjs` (19/19 passing, implementer-reported), covering whole-run atomicity, exact pointer/provenance no-op equivalence, and legacy-content-contract-aligned validation boundaries. Local Wrangler/D1 simulation only — no remote/production D1 resource exists; not the public source of truth (`app/page.js`/`lib/content/local.mjs` unchanged, still read `data/site.js`). |
| Persistent storage — media (R2) | `IMPLEMENTED` (local-only) | `WEB-INC-004` (`ML-DEVOS-RFC-007`/`ML-DEVOS-AS-023`/`D-029`): `wrangler.jsonc` adds a locally-simulated `MEDIA` R2 binding (`remote: false`, no real bucket/credential/custom domain/production identifier); `migrations/0003_web_inc_004_media.sql` adds exactly two new tables, `media` and `project_media` (current schema: 17 tables total — the 15 existing tables unchanged plus these two). Both tables are structurally immutable/non-deletable via database-level triggers except `media.state` (no code path in this increment ever changes it). No public media route, cutover, or deployment exists. |
| Media management/upload | `ARCHITECT_APPROVED` (local-only, admin-only; `ML-DEVOS-AS-027` / `ML-DEVOS-ADR-007`) | `WEB-INC-004`: `worker/admin/media.mjs` exposes exactly `POST /admin/api/media` and `GET /admin/api/media` — no update/delete route. Upload enforces auth + bounded subject + same-origin before validation; only `image/jpeg`/`image/png`/`image/webp` are accepted (`worker/media/signature.mjs` magic-byte detection, not `Content-Type` alone; SVG rejected by construction); real 5 MiB byte limit; server-generated media id/storage key (never client-supplied); local R2 write happens before the D1 batch (media row + `media_upload` success audit), with a best-effort compensating R2 delete if the D1 batch then fails (an orphaned object if that compensating delete itself also fails is a documented, non-blocking operational limitation, not fabricated D1 consistency). `POST /admin/api/projects` and `PUT /admin/api/projects/:id/draft` (`worker/d1/projects.mjs`, `worker/admin/projects.mjs`) now additionally accept an optional, complete, revision-scoped media snapshot for the new revision: supplying `media` fully replaces the new revision's association set; omitting it on edit inherits the source revision's associations so a text-only edit never silently drops media; referenced media must exist and be active; the whole revision + junction-insert + pointer-move + success-audit set commits as one D1 batch, and the existing `WEB-INC-003` commit-time stale-write guard is unmodified and still enforced. Preview (`GET /admin/api/projects/:id/preview`) now also returns bounded media metadata for the exact current draft revision only, never falling back to the published revision's associations. No admin UI exists for any of this — API surface only. `tests/worker-admin-media.test.mjs` (33/33) and WEB-INC-004 media integration coverage in `tests/worker-admin-projects.test.mjs` (63/63 total); full suite 209/209, implementer-reported. Final implementation/remediation accepted by `ML-DEVOS-AS-027`; architecture recorded by `ML-DEVOS-ADR-007`. |
| Audit logging | `IMPLEMENTED` (substrate + real-mutation integration, local-only) | `WEB-INC-008` (`ML-DEVOS-RFC-005`/`ML-DEVOS-AS-017`/`D-026`): `migrations/0002_web_inc_008_audit_log.sql` adds exactly one new table, `audit_log` (current schema: 15 tables total, the 14 `WEB-INC-005` tables unchanged plus this one); `worker/d1/audit.mjs` exposes `appendAuditEvent`/`buildAuditAppendStatement`/`buildProjectRevisionAuditStatement` (strict field allowlist, server-generated `occurred_at`, no update/delete helper). Append-only enforced independently at the database layer too, via `BEFORE UPDATE`/`BEFORE DELETE` triggers that unconditionally `RAISE(ABORT, ...)`. `ML-DEVOS-AS-019` accepted the repository/local implementation and `ML-DEVOS-ADR-005` records the architecture. `WEB-INC-003` (`ML-DEVOS-RFC-006`/`ML-DEVOS-AS-020`/`D-027`) is the first real admin action to call it: every project create/edit/publish/unpublish appends a `result: success` row atomically with the business mutation, and bounded failures append `result: failure`. `tests/d1-audit.test.mjs` (17/17) plus `tests/worker-admin-projects.test.mjs` (39/39, implementer-reported); full suite 152/152. |
| Deployment-wording contradiction (Architect finding F-003) | `IMPLEMENTED` | `AGENTS.md` and `README.md` updated this cycle to describe the Cloudflare Worker/Wrangler asset deployment instead of "Cloudflare Pages," matching `docs/ARCHITECTURE.md` and `wrangler.jsonc`. No infrastructure changed. |
| Legacy branch inventory (Architect finding F-004) | `IMPLEMENTED` | Recorded in `PROJECT_GOVERNANCE.md` § "Legacy / non-governance branch inventory," classified `UNINSPECTED LEGACY/EXPERIMENTAL`. No branch content was reviewed or merged. |

## Explicit non-claims

This document does not claim, and neither the Phase 1 cycle, `WEB-INC-005`, `WEB-INC-002`, `WEB-INC-008`, nor `WEB-INC-003` performed:

- Any change to public-facing functionality or visual design. `WEB-INC-003`'s local D1 "publish" does not change what the public site serves — `D1 PUBLISHED ≠ PRODUCTION WEBSITE LIVE` (`AS20-F015`); no public D1 cutover exists.
- Any admin content-editing UI, or any upload implementation. `WEB-INC-003` implements the project mutation API only — no admin UI control (new/edit/preview/publish/unpublish button, form, etc.) was added anywhere under `app/admin/`.
- Any project delete, slug rename, or mutation of any content domain other than `projects` (no navigation/foundations/services/process-step/section/site-settings mutation, no journal/theme mutation). `WEB-INC-004` adds exactly `media`/`project_media` as described above — media upload/list, and project media-snapshot association, only; no media update/delete endpoint exists, no project_media free-standing mutation endpoint exists, and no journal/theme table or route exists.
- (`WEB-INC-004`) Any real/remote R2 or D1 resource, public bucket, custom domain, or public media-serving route — `LOCAL R2 OBJECT + D1 MEDIA ROW != PUBLIC WEBSITE MEDIA` (`AS23-F015`); the public site's media continues to come only from `public/`/`data/site.js` exactly as before this increment. No Sentinel S3+/CI-rulesets/Capability-Gateway/Task-Engine/Orchestrator work was started (`AS23-F017`).
- Any HTTP audit-write/read endpoint or admin audit UI. `appendAuditEvent`/`buildAuditAppendStatement`/`buildProjectRevisionAuditStatement` are server-only internal functions; none is imported by any client-reachable path, and `GET /admin/api/dashboard`'s response is unchanged and exposes no audit or project-mutation data.
- Any persistent admin/user/session/role table. The audit `actor` remains a bounded opaque value derived only from the verified Access `sub` (`cf-access:<sub>`); no identity table exists.
- Any remote/production D1 resource creation, migration, query, or mutation, or any deployment or production verification.
- Any schema/migration change. `migrations/0001_web_inc_005_init.sql` and `migrations/0002_web_inc_008_audit_log.sql` are both byte-unchanged; the schema remains exactly 15 product tables.
- Any change to `app/page.js`, `lib/content/local.mjs`, or the public read path — it still reads only `data/site.js`; `data/site.js` was not deleted or retired.
- Any merge to `main`, or any work on `WEB-INC-004`/`006`/`007` or later increments.
- Independent (Architect-reproduced) verification of the test/build evidence cited above — those remain implementer-reported until the Architect inspects them (see `TEST_LEDGER.md`).
