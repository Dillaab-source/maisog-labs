# Governance Map

## Traceability model

```
Requirement → Design → Implementation → Test → Evidence → Status
```

Example (from the governance plan, §15):

```
ADM-REQ-004
  → Projects CMS design
  → /admin/projects + API + persistent storage
  → TEST-ADM-004
  → test/database evidence
  → VERIFIED
```

Code existence alone is never `VERIFIED`. A record only reaches `VERIFIED` when independently reproduced or inspected evidence (see the evidence-class rule in `PROJECT_GOVERNANCE.md` and `TEST_LEDGER.md`) supports it — implementer-reported evidence alone caps a record at `IMPLEMENTED`.

## Status vocabulary

`NOT STARTED` · `IN PROGRESS` · `IMPLEMENTED` · `VERIFIED` · `BLOCKED` · `DEFERRED`

## Current requirement status

The full requirement catalog (`WEB-REQ-*`, `ADM-REQ-*`, `DESIGN-*`, `WEB-SEC-*`) is defined in `docs/MAISOGLABS_WEBSITE_GOVERNANCE_ADMIN_PLAN_v0.1.txt` §§9–11, 13. `WEB-REQ-001`…`008` are broken out individually below because they do not all share the same status or evidence (Architect finding F1-003: an earlier draft of this table aggregated them under a single `IMPLEMENTED` label, which incorrectly implied `WEB-REQ-004` — admin-managed editing without source-code changes — was implemented when no Admin surface exists). As of `WEB-INC-001` (`ML-DEVOS-RFC-002`/`ML-DEVOS-AS-011`/`D-023`), `ADM-REQ-001` and `WEB-SEC-001`/`002`/`011` are likewise broken out individually, for the same reason: they no longer share the same status as the rest of their groups. `ADM-REQ-*`, `DESIGN-*`, and `WEB-SEC-*` statuses are split as capabilities are actually designed/implemented. `DESIGN-001`…`014` are implemented at repository/local level by `WEB-INC-007` and accepted by `ML-DEVOS-AS-033` / `ML-DEVOS-ADR-009`; production verification is not claimed. No requirement is upgraded to `VERIFIED` without independent evidence.

| Requirement | Design | Implementation | Test | Evidence | Status |
|---|---|---|---|---|---|
| `WEB-REQ-001` Public website remains available and usable | Existing (`app/page.js` renders `getPublicContent()` output) | Implemented for the current homepage-only scope | `tests/content.test.mjs` (content-boundary only) | Implementer-reported: `npm run build` success | `IMPLEMENTED` — not `VERIFIED` (no independently reproduced production/runtime availability check) |
| `WEB-REQ-002` Existing content must not disappear during unrelated changes | No automated safeguard designed | Upheld only by manual Git-diff review during each cycle (this cycle's own validation step) | None | This cycle's `git diff`/`git status` review before commit | `NOT STARTED` as an enforced technical control — currently a process practice, not a tested guarantee |
| `WEB-REQ-003` Mobile experience remains usable | Existing responsive CSS (`app/globals.css`) | Implemented, unverified | No automated mobile/visual test exists; `docs/CHANGE_LEDGER.md` records only past manual QA | Implementer-reported code inspection only | `IMPLEMENTED` (code exists) — not `VERIFIED`; matches `TEST-WEB-003: NOT IMPLEMENTED` in `TEST_LEDGER.md` |
| `WEB-REQ-004` Admin-managed public content must not require source-code edits | Design target recorded in `docs/product/DATA_BACKEND_SPEC.md` | **Not implemented for the public site.** `WEB-INC-003` (`ML-DEVOS-RFC-006`/`ML-DEVOS-AS-020`/`D-027`) now lets an authenticated admin create/edit/publish/unpublish a project entirely through the local D1 substrate, with no source-code edit — but `D1 PUBLISHED ≠ PRODUCTION WEBSITE LIVE` (`AS20-F015`): no public D1 cutover exists, so a local D1 "publish" does not change what the actual public site serves. The public build still reads only `data/site.js` | None | An auth-only `/admin` placeholder exists under `WEB-INC-001`, a local-only D1 revision-substrate/migration layer exists under `WEB-INC-005`, and `WEB-INC-003` now provides real project create/edit/publish/unpublish capability against that substrate — but the public site's content still requires a direct source edit and rebuild/redeploy, exactly as before every increment, since none of them perform a public-source cutover. Admin write capability existing is not the same as this requirement's target (admin-managed *public* content) being satisfied (`ML-DEVOS-AS-013`, `AS20-F015`) | `NOT STARTED` for the public-facing requirement; the local admin-side prerequisite (a real write path) is now `IMPLEMENTED` — see the `WEB-INC-003` row above |
| Current-content D1 revision substrate (`WEB-INC-005`) | `docs/product/DATA_BACKEND_SPEC.md` § "Publication / revision model"; `ML-DEVOS-RFC-003` | Implemented — exactly 14 local D1 tables (`migrations/0001_web_inc_005_init.sql`): `site_settings`/`navigation`/`foundations`/`projects`/`services`/`process_steps`/`sections`, each with a `_revisions` companion; base rows carry only identity/immutable metadata/pointers; cross-entity pointer ownership enforced via composite foreign keys; deterministic idempotent migration (`worker/d1/migrate.mjs`, `scripts/d1-migrate.mjs`) maps current `data/site.js` content and bootstraps the four managed sections | `tests/d1-migration.test.mjs` (12 tests: table inventory, deep parity, sections isolation, repeat-run determinism, refusal-on-changed-content, published/draft/archived fixture mapping, draft-reorder isolation, cross-entity pointer rejection, slug uniqueness/reserved-slug, revision-number uniqueness, FK integrity) | Implementer-reported: 12/12 passing; deep-equal parity confirmed against `projectPublishedContent(siteContent)`; local-only `wrangler d1 migrations apply --local`/`wrangler d1 execute --local` CLI evidence recorded in `coordination/IMPLEMENTER_HANDOFF.md` | `IMPLEMENTED` — not `VERIFIED` (no Architect-reproduced or production/runtime evidence); local-only, not the public source of truth (`app/page.js` still reads `data/site.js`) |
| `WEB-REQ-005` Website renders content from a structured content layer | Existing, documented in `PROJECT_GOVERNANCE.md` | Implemented (`data/site.js → schema.mjs → public.mjs → local.mjs → app/page.js`) | `tests/content.test.mjs` | Implementer-reported: 27/27 passing | `IMPLEMENTED` |
| `WEB-REQ-006` Site remains visually consistent with MaisogLabs branding | Existing (`app/globals.css`, `components/Logo.js`) | Implemented | No automated design-consistency test | Implementer-reported code inspection only | `IMPLEMENTED` — not `VERIFIED` (no independent design review) |
| `WEB-REQ-007` Site remains lightweight/performance-conscious | Existing (static export, optimized WebP asset per `docs/CHANGE_LEDGER.md`) | Implemented | No automated performance/Lighthouse test exists | Implementer-reported: static build succeeds; no perf budget test | `IMPLEMENTED` — not `VERIFIED` |
| `WEB-REQ-008` Public site never exposes admin-only controls/data | `lib/content/schema.mjs` rejects unknown fields (e.g. an `adminEmail`-style field) | Implemented | `tests/content.test.mjs` — "rejects unknown admin field" | Implementer-reported PASS | `IMPLEMENTED` |
| `ADM-REQ-001` `/admin` requires authentication | `ML-DEVOS-RFC-002` § "Add a minimal Worker authentication boundary" | Implemented (`worker/index.mjs`, `worker/auth.mjs`) — fail-closed Cloudflare Access JWT verification gates `/admin`, `/admin/*`; Remediation Cycle 1 (`ML-DEVOS-AS-012`) additionally makes the auth configuration itself fail closed and pins/proves alternate-URL canonicalization | `tests/worker-auth.test.mjs` (30 tests) | Implementer-reported: 57/57 passing (full suite); local `wrangler dev` smoke test confirms `GET /admin` → 401 without a token and `GET /admin.html` → redirect → 401, never direct admin content | `IMPLEMENTED` — not `VERIFIED` (no Architect-reproduced or production/runtime evidence yet; no production Cloudflare Access application exists) |
| `ADM-REQ-005` Journal lifecycle | `ML-DEVOS-RFC-009` / `ML-DEVOS-AS-028` | Implemented locally by `WEB-INC-006` (create/edit/preview/publish/unpublish; no delete) | `tests/worker-admin-journal.test.mjs` | Builder 44/44; source/diff independently inspected in `ML-DEVOS-AS-029` | `IMPLEMENTED` — local/repository only |
| `ADM-REQ-007` Section visibility | `ML-DEVOS-RFC-010` uses existing `sections`/`section_revisions` draft/publish model | Implemented by WEB-INC-007 | `tests/worker-admin-design.test.mjs` + final full suite | Source/diff independently inspected in `ML-DEVOS-AS-033`; runtime evidence ACTOR_REPORTED | `IMPLEMENTED` — local/repository only |
| `ADM-REQ-008` Section order | `ML-DEVOS-RFC-010` uses existing `sections`/`section_revisions` draft/publish model | Implemented by WEB-INC-007 | `tests/worker-admin-design.test.mjs` + final full suite | Source/diff independently inspected in `ML-DEVOS-AS-033`; runtime evidence ACTOR_REPORTED | `IMPLEMENTED` — local/repository only |
| `ADM-REQ-009` Approved brand/theme settings | `ML-DEVOS-RFC-010` fixed presets/ranges only | Implemented by WEB-INC-007 | `TEST-ADM-009: PASS`; design suites + final full suite | `ML-DEVOS-AS-033` independently inspected final source/diff; runtime evidence ACTOR_REPORTED | `IMPLEMENTED` — local/repository only |
| `ADM-REQ-006` Admin can upload/select approved media | `ML-DEVOS-RFC-007` § "Proposed architecture"; `ML-DEVOS-AS-023` | `WEB-INC-004` implemented (local-only, API-only — no admin UI): `worker/admin/media.mjs` exposes `POST /admin/api/media` (upload) and `GET /admin/api/media` (select-from list) — both dispatched only after `worker/auth.mjs`'s fail-closed Access verification succeeds, upload additionally gated on a bounded non-empty verified `sub`. Only `image/jpeg`/`image/png`/`image/webp` are accepted, validated by magic-byte signature rather than trusted `Content-Type` alone; SVG and any other type is rejected. `POST /admin/api/projects`/`PUT /admin/api/projects/:id/draft` extend project create/edit with an optional, atomic, revision-scoped media selection (`{mediaId, role, order}`), satisfying the "select approved media" half of this requirement for the one content domain (`projects`) this cycle touches | `tests/worker-admin-media.test.mjs` (22 tests); `tests/worker-admin-projects.test.mjs` media-snapshot integration tests (9 new) | Implementer-reported: 195/195 passing (full suite) | `IMPLEMENTED` at the repository/local level for projects only — not `VERIFIED` (no Architect-reproduced or production/runtime evidence yet); no admin UI exists, and no other content domain accepts a media selection |
| `ADM-REQ-002` Only authorized admins may change site content; `ADM-REQ-003` Admin changes persist; `ADM-REQ-004` Projects support create/edit/publish/unpublish/delete (**partial** — no delete); `ADM-REQ-010` Admin can preview changes before publishing; `ADM-REQ-011` Invalid input is rejected safely; `ADM-REQ-013` Admin fields cannot inject arbitrary executable browser code; `ADM-REQ-014` Draft vs published state is supported; `ADM-REQ-015` Save/publish feedback is explicit; `ADM-REQ-016` Failed writes never silently appear successful | `ML-DEVOS-RFC-006` § "Proposed architecture"; `ML-DEVOS-AS-020` | `WEB-INC-003` implemented (local-only): `worker/admin/projects.mjs` dispatches the five authorized routes only after `worker/auth.mjs`'s fail-closed Access verification succeeds AND (for mutating routes) a bounded non-empty verified `sub` exists (`ADM-REQ-002`); `worker/d1/projects.mjs`'s batched writes persist create/edit/publish/unpublish atomically with a success audit event (`ADM-REQ-003`, `016`); create/edit/publish/unpublish exist, delete does not (`ADM-REQ-004` partial); `GET .../preview` returns the current draft before publish (`ADM-REQ-010`); every field is validated server-side via the unmodified `worker/d1/validate.mjs`, rejecting malformed/unknown content with a bounded `400` and no DB write (`ADM-REQ-011`, `013`); lifecycle state (`draft`/`published`/`published_with_draft`/`archived`) is derived from the two pointers exactly as `WEB-INC-002` already established (`ADM-REQ-014`); every mutation response positively constructs bounded status fields including the new/updated revision id (`ADM-REQ-015`); a storage/transaction failure is proven to leave no partial mutation and never returns a success status (`ADM-REQ-016`) | `tests/worker-admin-projects.test.mjs` (51 tests after remediation: original project-mutation coverage plus commit-time TOCTOU interleavings, bounded-subject boundaries, UTF-8 byte-budget checks, and route/DB-ordering regressions) | Implementer-reported: 164/164 passing (full suite); exact implementation/remediation diffs and focused test source independently inspected by Architect | `ARCHITECT_APPROVED` at repository/local level via `ML-DEVOS-AS-022`; production/runtime remains unverified and no production Cloudflare Access application exists |
| Protected read-only admin dashboard (`WEB-INC-002`) | `ML-DEVOS-RFC-004` § "Proposed architecture"; `ML-DEVOS-AS-015` | Implemented — `worker/admin/dashboard.mjs` dispatches `GET /admin/api/dashboard` only after `worker/auth.mjs`'s existing WEB-INC-001 fail-closed Access verification succeeds; returns an explicit allowlisted status projection (id, slug where applicable, derived lifecycle state, published/draft revision IDs, bounded display label, sections-only order/visible) for `site_settings`/`navigation`/`foundations`/`projects`/`services`/`process_steps`/`sections`, reading `worker/d1/repository.mjs`'s bounded functions. No mutation, session, or identity-persistence capability of any kind | `tests/worker-admin-dashboard.test.mjs` (20 tests: zero-D1-invocation on invalid auth, lifecycle fixture coverage for all four states, allowlist leakage tests, 405/404/503/500 handling, cache/content-type/CORS hardening, read-only-SQL proof); `tests/worker-auth.test.mjs` (30 tests, unchanged, still passing) | Implementer-reported: 96/96 passing (full suite); local `wrangler dev` smoke test confirms `GET /admin/api/dashboard` (no token) → 401 with `Cache-Control: no-store`, matching existing `/admin` behavior | `IMPLEMENTED` — not `VERIFIED` (no Architect-reproduced or production/runtime evidence yet; no production Cloudflare Access application exists) |
| `ADM-REQ-012` Important admin actions are auditable | `docs/product/DATA_BACKEND_SPEC.md` § `audit_log`; `ML-DEVOS-RFC-005` | Substrate implemented (`WEB-INC-008`), integration not yet applicable — `migrations/0002_web_inc_008_audit_log.sql` adds exactly one new table, `audit_log` (current schema: 15 tables — the 14 `WEB-INC-005` tables unchanged plus this one); `worker/d1/audit.mjs` exposes only `appendAuditEvent(db, event)`, which validates via a strict field allowlist, generates `occurred_at` itself, and propagates any storage failure by throwing (`ADM-REQ-016`/`WEB-SEC-012`: never reports success on a failed write). `WEB-INC-003` (`ML-DEVOS-RFC-006`/`ML-DEVOS-AS-020`/`D-027`) is that integration: every successful create-draft/edit-draft/publish/unpublish appends exactly one success `audit_log` row atomically with the business mutation, and bounded authenticated failures append a `result: failure` row | `tests/d1-audit.test.mjs` (17 tests: the original 16 audit-substrate checks plus the AS18-F014 repeat-safety regression); `tests/worker-admin-projects.test.mjs` (every create/edit/publish/unpublish success/failure test asserts the exact expected audit row; the atomicity test proves a forced audit failure rolls back the paired business mutation) | Implementer-reported final WEB-INC-003 evidence: 164/164 passing (full suite); `WEB-INC-008` prior remediation evidence remains unchanged; exact WEB-INC-003 implementation/remediation diffs and focused remediation tests independently inspected by Architect | `ARCHITECT_APPROVED` for both the audit substrate (`ML-DEVOS-AS-019`/`ML-DEVOS-ADR-005`) and this exact project-mutation integration (`ML-DEVOS-AS-022`); `RISK-WEB-014` is `MITIGATED` for this exact mutation set |
| `DESIGN-001`…`014` (admin design controls) | `ML-DEVOS-RFC-010` — fixed preset/enum controls, bounded numeric ranges, section revisions for visibility/order, theme revisions for remaining design state, published-only public `/api/design`, no arbitrary CSS/JS/HTML/URLs | Implemented by WEB-INC-007, including authenticated visual draft preview and screenshot-reference workflow | `TEST-ADM-009: PASS`; 338/338 final full suite Builder-reported | Final source/diff accepted by `ML-DEVOS-AS-033`; `ML-DEVOS-ADR-009` | `IMPLEMENTED` — local/repository only; not production-verified |
| `WEB-SEC-001` `/admin` requires authenticated access | `ML-DEVOS-RFC-002` §§1–2 | Implemented — Worker-first routing restricted to `/admin`/`/admin/*` only (`wrangler.jsonc` `assets.run_worker_first`); ordinary routes remain asset-first, confirmed unchanged; alternate URL forms (`/admin.html`, `/admin/index.html`) confirmed non-bypassing via pinned `html_handling` (`AS12-F003`) | `tests/worker-auth.test.mjs` ("serves ordinary public routes asset-first..."); local `wrangler dev`: `GET /` → 200, `GET /nope` → 404, `GET /admin.html` → 307 redirect (no content) → 401, unchanged/confirmed | Implementer-reported PASS; local dev smoke test | `IMPLEMENTED` — not `VERIFIED` |
| `WEB-SEC-002` Authorization checked server-side | `ML-DEVOS-RFC-002` § "Add a minimal Worker authentication boundary" | Implemented — JWT verified server-side in the Worker (`jose`), never trusting a client-supplied claim; Remediation Cycle 1 additionally validates the server-side auth configuration itself before any verification is attempted (`AS12-F001`) | `tests/worker-auth.test.mjs` | Implementer-reported PASS | `IMPLEMENTED` — not `VERIFIED` |
| `WEB-SEC-011` Authentication failure fails closed | `ML-DEVOS-AS-011` `AS11-F002`, critical invariant "NO VALID SERVER-VERIFIED ACCESS IDENTITY → NO ADMIN ASSET"; extended by `ML-DEVOS-AS-012` `AS12-F001` to cover configuration itself | Implemented — missing/malformed/expired/wrong-audience/wrong-issuer/untrusted-key assertions **and** missing/blank/placeholder/malformed team-domain/audience configuration all return `401` before any asset is served or any JWKS/network lookup is attempted | `tests/worker-auth.test.mjs` (7 token-level + 9 config-level negative-path cases) | Implementer-reported PASS; local `wrangler dev`: `GET /admin` with no token and with a garbage token both → 401 | `IMPLEMENTED` — not `VERIFIED` |
| `WEB-SEC-003`…`010`, `012` (remaining auth/security boundary) | Not designed | Not implemented — no session storage, no mutation endpoint, no media upload, no audit log exists yet | None | None | `NOT STARTED` — explicitly **not** "not applicable"; these remain real, open requirements once the corresponding surface is built |
| Content contract (`data/site.js` → `schema.mjs` → `public.mjs` → `local.mjs`) | Existing, documented in `docs/CONTENT.md` and `PROJECT_GOVERNANCE.md` | Implemented | `tests/content.test.mjs` (27 tests: filtering, ordering, schema rejection, unsafe-link/HTML rejection, unpublished-root rejection) | Implementer-reported pass; not yet Architect-reproduced | `IMPLEMENTED` |
| `WEB-REQ-009` / Journal | WEB-INC-006: revisioned journal entities; published-only public index/detail; immutable slug lookup; newest-published-first ordering | Implemented at repository/local level | Journal admin/public suites; full suite 269/269 builder-reported | `ML-DEVOS-AS-029` independently inspected source/diff; runtime evidence remains ACTOR_REPORTED | `IMPLEMENTED` — local/repository only; no production verification |

## Requirement ID reservation

This bootstrap does not invent new requirement IDs. Future phases that design and implement specific `ADM-REQ-*`/`DESIGN-*`/`WEB-SEC-*` items should add rows here (or a per-domain sub-table) at the point of design, not before, and must cite the implementing file(s) and the test(s) that exercise them.
